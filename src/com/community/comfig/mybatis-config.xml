<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration PUBLIC "-I/mybatis.orgl/DTD Config 3.01IEN"
" http://mybatis.org/dtd/mybat~s-3-config.dtd">
<configuration>
	<!-- 配置环境，默认的环境id 为mysql -->
	<environments default="mysql">
		<!-- 配置id 为mysql 的数据库环境 -->
		<environment id="mysql">
			<!-- 使用JDBC 的事务管理 -->
			<transactionManager type="JDBC"/>
			<!-- 数据库连接池 -->
			<dataSource type="POOLED">
				<property name="driver" value="com.mysql.jdbc.Driver"/>
				<property name="url" value="jdbc:mysql://localhost:3306/mybatis"/>
				<property name="username" value="armon" />
				<property name="password" value="1510120036" />
			</dataSource>
		</environment>
	</environments>
	<!-- < !一配置别名一〉 -->
	<typeAliases>
		<typeAliases alias="User" type="com.community.bean.User"/>
	</typeAliases>
	<!-- < !--配置Mapper 的位置一〉 -->
	<mappers>
		<mapper resource="com/community/mapper/UserMapper.xml"/>
	</mappers>
</configuration>